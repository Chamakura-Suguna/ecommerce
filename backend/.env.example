PORT=4000
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/wishlist-storefront
# Comma-separated list of allowed browser origins. Include the deployed
# GitHub Pages origin alongside local dev ports.
CORS_ORIGIN=http://localhost:5500,http://127.0.0.1:5500,https://chamakura-suguna.github.io
